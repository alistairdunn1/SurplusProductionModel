% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference-points.R
\name{calculate_reference_points}
\alias{calculate_reference_points}
\title{Calculate Reference Points from Fitted Model}
\usage{
calculate_reference_points(
  model_fit,
  biomass_target = NULL,
  baseline = c("auto", "B0", "K")
)
}
\arguments{
\item{model_fit}{A fitted ProductionModel object (with fitted = TRUE)}

\item{biomass_target}{Optional numeric vector of target biomass fractions.
For example, \code{0.4} requests \verb{B_40\%B0} and \verb{F_40\%B0} style reference
points. If omitted, uses package default from
\code{get_reference_point_defaults()}.}

\item{baseline}{Character string indicating which biomass baseline to use
for \code{biomass_target}: \code{"auto"} (default), \code{"B0"}, or \code{"K"}. In
\code{"auto"} mode, the function uses fitted \code{B0} if available and otherwise
falls back to \code{K}. If omitted, uses package default from
\code{get_reference_point_defaults()}.}
}
\value{
Named list containing reference points:
\describe{
\item{msy}{Maximum sustainable yield (tonnes)}
\item{bmsy}{Biomass at MSY (tonnes)}
\item{fmsy}{Fishing mortality at MSY (per year)}
\item{target_reference_points}{Optional data frame of user-defined
biomass and fishing mortality targets for requested biomass fractions.}
\item{current_status}{List with current biomass and status relative to reference points}
}
}
\description{
Calculates biological reference points (MSY, BMSY, FMSY) from a fitted
Pella-Tomlinson surplus production model.
}
\details{
Reference points are calculated using the Pella-Tomlinson formulation:

MSY = r * K * (m-1)^((m-1)/m) / m

BMSY = K * (m-1)^(1/m) / m

FMSY = MSY / BMSY

For user-defined biomass fractions \code{x}, the function also calculates
analytical depletion-based targets:

B_x = x * B_base

F_x = r / m * (1 - (B_x / K)^(m - 1))

where \code{B_base} is either fitted \code{B0} or \code{K}, depending on \code{baseline}.

For special cases:
\itemize{
\item Schaefer model (m=2): MSY = r*K/4, BMSY = K/2
\item Fox model (m=1): Uses limiting case formulation
}

Current status is calculated relative to reference points using the
final year biomass estimate from the fitted model.
}
\examples{
\dontrun{
# Assuming you have a fitted model
ref_points <- calculate_reference_points(fitted_model)
print(ref_points)

# Extract specific reference points
msy <- ref_points$msy
bmsy <- ref_points$bmsy

# Calculate B_40\%B0 and F_40\%B0 style targets
target_ref_points <- calculate_reference_points(
  fitted_model,
  biomass_target = 0.4,
  baseline = "B0"
)
}

}
\seealso{
\code{\link{fit_pella_tomlinson_model}} for model fitting
}
